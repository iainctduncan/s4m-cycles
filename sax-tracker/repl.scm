

(sum (vector 1 2 3))
(post "hello")
(apply + (list 1 2 3))


(sort! #(1 3 2) <)

(define v #(3 1 2))

(sort v <)
(copy #(1 2 3))
(make-vector 7 0)

(define rbuf-size 7)

(let ((rbuf (make-vector rbuf-size 0))

(floor (/ 7 2))


(+ (* 12 (log (/ 196 220) 2)) 57)

(round 3.4)

buf
(dotimes (i (length buf)) 
  (post i (buf i) "    - " (round (cps->midi (buf i)))))

(map (lambda(x)(+ 1 x)) #(1 2 3))

(map cps->midi buf)
buf
